/**
 * @category Client
 * @module
 */


/**
 * @description 
 * @returns 
 */
const parseSsotToBpmn = (modeler) => {
    const {cli} = window;
    //cli.elements();
    
    return 'Your BPMN could be here';
};

module.exports = { parseSsotToBpmn };